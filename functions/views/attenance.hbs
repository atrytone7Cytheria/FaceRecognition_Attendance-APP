<style>
.mytiles {
  display: inline-block;
  width: 50%;
  border:#621c1c
}
body {
  background: linear-gradient(35deg, red, purple);
}

/* <select> styles */
select {
  /* Reset */
  -webkit-appearance: none;
     -moz-appearance: none;
          appearance: none;
  border: 0;
  outline: 0;
  font: inherit;
  /* Personalize */
  width: 20em;
  height: 3em;
  padding: 0 4em 0 1em;
  background: url(https://upload.wikimedia.org/wikipedia/commons/9/9d/Caret_down_font_awesome_whitevariation.svg) no-repeat right 0.8em center/1.4em, linear-gradient(to left, rgba(255, 255, 255, 0.3) 3em, rgba(255, 255, 255, 0.2) 3em);
  color: white;
  border-radius: 0.25em;
  box-shadow: 0 0 1em 0 rgba(0, 0, 0, 0.2);
  cursor: pointer;
  /* <option> colors */
  /* Remove focus outline */
  /* Remove IE arrow */
}
select option {
  color: inherit;
  background-color: #320a28;
}
select:focus {
  outline: none;
}
select::-ms-expand {
  display: none;
}

.searchdropdown 
{
  border: 1px solid #777;
  border-radius: 4px;
  margin-bottom: 10px;
  padding: 0 5px 5px;
  background-color:#ebebeb;
  margin: 3%;
}

.searchdropdown label
{
  font-size:10px;
  text-transform: uppercase;
  color: #a927ae;
  font-weight: bolder;
  padding: 2px 8px 0;
}

select
{
  background-color: #282222;
  border:0px;
}

/* unvisited link */
a:link {
  color: red;
}

/* visited link */
a:visited {
  color: green;
}

/* mouse over link */
a:hover {
  color: rgb(5, 3, 31), 105, 255);
}

/* selected link */
a:active {
  color: blue;
}
</style>

<!--        <a href="StudentList"
                    onclick="javascript:window.demo.loadContent('studentlist');return false;">
                    All Students
                </a>
                -->


  <div>

  </div>
